#!/bin/bash
# Fixes ownership (pkgbuild's "recommended" ownership should already do this,
# belt-and-suspenders) and reloads coreaudiod so the driver is picked up.
chown -R root:wheel /Library/Audio/Plug-Ins/HAL/VirtualAudioInterfaceDriver.driver
killall coreaudiod 2>/dev/null || true
exit 0
